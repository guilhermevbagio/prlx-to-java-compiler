/* This file was generated by SableCC (http://www.sablecc.org/). */

package prolixa.node;

import java.util.*;
import prolixa.analysis.*;

@SuppressWarnings("nls")
public final class AMatrizTamanhos extends PTamanhos
{
    private final LinkedList<TNumero> _numero_ = new LinkedList<TNumero>();

    public AMatrizTamanhos()
    {
        // Constructor
    }

    public AMatrizTamanhos(
        @SuppressWarnings("hiding") List<?> _numero_)
    {
        // Constructor
        setNumero(_numero_);

    }

    @Override
    public Object clone()
    {
        return new AMatrizTamanhos(
            cloneList(this._numero_));
    }

    @Override
    public void apply(Switch sw)
    {
        ((Analysis) sw).caseAMatrizTamanhos(this);
    }

    public LinkedList<TNumero> getNumero()
    {
        return this._numero_;
    }

    public void setNumero(List<?> list)
    {
        for(TNumero e : this._numero_)
        {
            e.parent(null);
        }
        this._numero_.clear();

        for(Object obj_e : list)
        {
            TNumero e = (TNumero) obj_e;
            if(e.parent() != null)
            {
                e.parent().removeChild(e);
            }

            e.parent(this);
            this._numero_.add(e);
        }
    }

    @Override
    public String toString()
    {
        return ""
            + toString(this._numero_);
    }

    @Override
    void removeChild(@SuppressWarnings("unused") Node child)
    {
        // Remove child
        if(this._numero_.remove(child))
        {
            return;
        }

        throw new RuntimeException("Not a child.");
    }

    @Override
    void replaceChild(@SuppressWarnings("unused") Node oldChild, @SuppressWarnings("unused") Node newChild)
    {
        // Replace child
        for(ListIterator<TNumero> i = this._numero_.listIterator(); i.hasNext();)
        {
            if(i.next() == oldChild)
            {
                if(newChild != null)
                {
                    i.set((TNumero) newChild);
                    newChild.parent(this);
                    oldChild.parent(null);
                    return;
                }

                i.remove();
                oldChild.parent(null);
                return;
            }
        }

        throw new RuntimeException("Not a child.");
    }
}
