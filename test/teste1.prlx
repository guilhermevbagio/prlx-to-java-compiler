This is program ExemploDois.

It presents:
    vector of number(5) Notas;
    alterable number I_;
    alterable number Soma;
    alterable number Media;
    unalterable answer Aprovado;

Let us begin:
    Soma := 0;
    I_ := 1;

    show("Digite as 5 notas:");

    as long as (I_ <= 5) do [
        capture(Notas(I_));
        Soma := Soma + Notas(I_);
        I_ := I_ + 1;
    ]

    Media := Soma / 5;

    Aprovado = Media >= 6;

    just in case that (Aprovado == yes) do
        show("Aprovado com média:", Media);
    otherwise do
        show("Reprovado com média:", Media);
That would be all.